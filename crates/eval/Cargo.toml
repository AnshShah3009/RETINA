[package]
name = "cv-eval"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Evaluation metrics for visual localization, SfM and SLAM: trajectory ATE/RPE, reconstruction quality and retrieval metrics"

[dependencies]
cv-core = { workspace = true }
nalgebra = { workspace = true }

[features]
default = []
